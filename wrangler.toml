# The deployed demo site: static assets only, no Worker script. `site/` is
# generated by `npm run build:site` and committed, so a deploy is one command.
name = "webmcp"
compatibility_date = "2026-09-01"

[assets]
directory = "./site"
